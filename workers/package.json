{
  "name": "flowkit-deploy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Flow Kit deployment portal",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.2.3",
    "ssh2": "^1.16.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
