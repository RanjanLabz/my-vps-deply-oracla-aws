// Flow Kit — Deploy Portal Configuration
// Update this URL after deploying the Cloudflare Worker

window.DEPLOY_CONFIG = {
  // Replace with your Cloudflare Worker URL after running:
  //   cd workers && npm install && npx wrangler deploy
  WORKER_URL: ''
};
